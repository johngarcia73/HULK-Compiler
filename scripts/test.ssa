# Define the string in the data section
data $msg = align 8{ b "Result is %d \n", b 0}
data $hello = align 8{ b "Hello, World!\n", b 0}
function l $add(l %a, l %b) {
@start
    %result = l add %a, %b
    ret %result
}

export function w $main() {
@start
        # int sum = 0;
        # int i = 0;
        # while (i < 10) {
        #
        #   sum+=i;
        #   i++;
        #}
    
        
        %sum = l copy 0
        %i = l copy 0 
        @cond
            %cond = l csltl %i, 10
            jnz %cond,@loop,@end
        @loop
           %sum = l add %sum, %i
           %i = l add %i, 1
           jmp @cond
        @end
            call $printf(l $msg,..., l %sum)
        ret 0
    
}
